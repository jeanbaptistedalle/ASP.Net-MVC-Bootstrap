@model TestApp2.ViewsModels.Auth.LoginModel
@{
    ViewBag.Title = "Log In";
}

<h2>Log In</h2>

@using (Html.BeginForm())
{

    @Html.EditorForModel()
    <br />
    <p>
        <button type="submit">Connection</button>
    </p>
    <a href="@Url.Action("register", "auth")">S'inscrire</a>
}