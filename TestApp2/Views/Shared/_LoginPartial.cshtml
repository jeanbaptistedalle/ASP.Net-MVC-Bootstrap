@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    <form method="post">
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("Bonjour " + User.Identity.GetUserName(), "Index", "home")
            </li>
            <li>@Html.ActionLink("Se déconnecter", "Logout","auth")</li>
        </ul>
    </form>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("S’inscrire", "Register", "auth", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Se connecter", "Login", "auth", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
